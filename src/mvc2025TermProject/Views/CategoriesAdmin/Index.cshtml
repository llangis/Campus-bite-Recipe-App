@model PaginatedList<mvc2025TermProject.Models.CategoryAdminListViewModel>

@{
    ViewData["Title"] = "Category Review";
}

<div class="container my-3 my-md-4">
    <div class="position-relative">
        <div class="auth-accent rounded-top"></div>

        <div class="bg-white border rounded-bottom shadow-sm overflow-hidden">

            <partial name="_AdminSearchFilterPartials" />

            <div class="table-responsive px-4 pb-3">
                <table class="table table-borderless table-hover align-middle mb-0">
                    <thead class="text-nowrap">
                        <tr>
                            <th>
                                <a asp-action="Index"
                                   asp-route-sortOrder="@ViewData["NameSortParam"]"
                                   asp-route-filterCriteria="@ViewData["SearchFilterCriteria"]"
                                   asp-route-statusFilter="@ViewData["CurrentStatus"]"
                                   class="text-decoration-none text-dark">
                                    @Html.DisplayNameFor(model => model.First().Name)
                                    @if ((string)ViewData["NameSortParam"]! == "name_desc")
                                    {
                                        <i class="bi bi-sort-down text-color-eb"></i>
                                    }
                                    else if ((string)ViewData["NameSortParam"]! == null)
                                    {
                                        <i class="bi bi-sort-up text-color-eb"></i>
                                    }
                                    else
                                    {
                                        <i class="bi bi-filter text-color-eb"></i>
                                    }
                                </a>
                            </th>
                            <th>
                                <a asp-action="Index"
                                   asp-route-sortOrder="@ViewData["RequestTypeSortParam"]"
                                   asp-route-filterCriteria="@ViewData["SearchFilterCriteria"]"
                                   asp-route-statusFilter="@ViewData["CurrentStatus"]"
                                   class="text-decoration-none text-dark">
                                    @Html.DisplayNameFor(model => model.First().RequestType)
                                    @if ((string)ViewData["RequestTypeSortParam"]! == "request_desc")
                                    {
                                        <i class="bi bi-sort-down text-color-eb"></i>
                                    }
                                    else if ((string)ViewData["RequestTypeSortParam"]! == "request")
                                    {
                                        <i class="bi bi-sort-up text-color-eb"></i>
                                    }
                                    else
                                    {
                                        <i class="bi bi-filter text-color-eb"></i>
                                    }
                                </a>
                            </th>
                            <th>@Html.DisplayNameFor(model => model.First().CreatedBy)</th>
                            <th>
                                <a asp-action="Index"
                                   asp-route-sortOrder="@ViewData["DateSortParam"]"
                                   asp-route-filterCriteria="@ViewData["SearchFilterCriteria"]"
                                   asp-route-statusFilter="@ViewData["CurrentStatus"]"
                                   class="text-decoration-none text-dark">
                                    @Html.DisplayNameFor(model => model.First().CreatedDate)
                                    @if ((string)ViewData["DateSortParam"]! == "date_desc")
                                    {
                                        <i class="bi bi-sort-down text-color-eb"></i>
                                    }
                                    else if ((string)ViewData["DateSortParam"]! == "date")
                                    {
                                        <i class="bi bi-sort-up text-color-eb"></i>
                                    }
                                    else
                                    {
                                        <i class="bi bi-filter text-color-eb"></i>
                                    }
                                </a>
                            </th>
                            <th>
                                <a asp-action="Index"
                                   asp-route-sortOrder="@ViewData["StatusSortParam"]"
                                   asp-route-filterCriteria="@ViewData["SearchFilterCriteria"]"
                                   asp-route-statusFilter="@ViewData["CurrentStatus"]"
                                   class="text-decoration-none text-dark">
                                    @Html.DisplayNameFor(model => model.First().ReviewStatus)
                                    @if ((string)ViewData["StatusSortParam"]! == "status_desc")
                                    {
                                        <i class="bi bi-sort-down text-color-eb"></i>
                                    }
                                    else if ((string)ViewData["StatusSortParam"]! == "status")
                                    {
                                        <i class="bi bi-sort-up text-color-eb"></i>
                                    }
                                    else
                                    {
                                        <i class="bi bi-filter text-color-eb"></i>
                                    }
                                </a>
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model is null || !Model.Any())
                        {
                            <tr class="border-top">
                                <td colspan="5" class="text-center py-4 text-muted">No categories found.</td>
                            </tr>
                        }
                        else
                        {
                            foreach (var item in Model)
                            {
                                <tr class="border-top">
                                    <td class="fw-semibold">
                                        <a asp-action="Details" asp-route-id="@item.ID" class="text-decoration-none text-color-eb">
                                            @Html.DisplayFor(modelItem => item.Name)
                                        </a>
                                    </td>
                                    <td class="text-nowrap">@Html.DisplayFor(modelItem => item.RequestType)</td>
                                    <td class="text-nowrap">@Html.DisplayFor(modelItem => item.CreatedBy)</td>
                                    <td class="text-nowrap">@Html.DisplayFor(modelItem => item.CreatedDate)</td>
                                    <td class="text-nowrap">
                                        @if (item.ReviewStatus == "Approved")
                                        {
                                            <span class="badge bg-success">Approved</span>
                                        }
                                        else if (item.ReviewStatus == "Pending Approval")
                                        {
                                            <span class="badge bg-secondary">Pending Approval</span>
                                        }
                                        else if (item.ReviewStatus == "Pending Update")
                                        {
                                            <span class="badge bg-warning text-dark">Pending Update</span>
                                        }
                                        else if (item.ReviewStatus == "Rejected")
                                        {
                                            <span class="badge bg-danger">Rejected</span>
                                        }
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>

            <partial name="_PaginationPartial" model="Model" />

        </div>
    </div>
</div>